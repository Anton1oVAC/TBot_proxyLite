version: '3.8'
services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./db.sqlite3:/usr/src/app/db.sqlite3
      - ./bot.log:/usr/src/app/bot.log
